Stealth: TLS/JA3 fingerprint + coherence (got-scraping) + CAPTCHA detection
[CORRECTED v2 — fixes the tsc error from the first version]

FIX IN THIS VERSION: removed named imports of got-scraping error classes
(RequestError/HTTPError/TimeoutError) — they exist at runtime but are NOT in
got-scraping's type defs, so tsc failed even though tests passed. Now detects
HTTP errors structurally (err.response shape). tsc + build + 205 tests all green.

CHANGED:
  src/fetcher.ts          fetchFast: axios → got-scraping (real browser TLS/JA3).
                          Structural error detection (no type-export dependency).
                          Coherent Persona system.
  src/block-detector.ts   (piece 1) soft vs interactive block classification.

REMOVED:
  src/got-scraping.d.ts   fake type shim — DELETE THIS FILE if it's still in your
                          project (robocopy won't remove it): del src\got-scraping.d.ts

APPLY:
  robocopy over project, then:  del src\got-scraping.d.ts
  npm install
  npx tsc --noEmit    ← must be silent (this is what failed before)
  npx vitest run      ← expect 205/205

VERIFY ON YOUR MACHINE (sandbox can't): hit a TLS checker to confirm JA3 looks
like Chrome:  npx tsx src/cli.ts scrape https://tls.peet.ws/api/all "ja3 and user agent"
