// rebrowser-patches.d.ts — type shim for rebrowser-patches
// The package has no @types, so we declare it manually.
declare module "rebrowser-patches" {
  export function applyRebrowserPatches(): void;
}
